# Cloudflare Pages 构建配置

# 构建命令
BUILD_COMMAND="docker build -f Dockerfile.cloudflare -t github-follow-tool ."

# 部署命令 (Cloudflare Pages通常自动处理)
DEPLOY_COMMAND="echo 'Deployment completed'"

# 构建输出目录
BUILD_OUTPUT_DIRECTORY="/app/frontend"

# 环境变量
NODE_VERSION="18"
PYTHON_VERSION="3.9"
DEBUG="false"
LOG_LEVEL="INFO"
